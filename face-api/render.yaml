services:
  - type: web
    name: faceattend-face-api
    env: docker
    plan: starter # free tier sleeps after inactivity; upgrade for always-on
    dockerfilePath: ./Dockerfile
    envVars:
      - key: FACE_API_KEY
        generateValue: true
    healthCheckPath: /health
